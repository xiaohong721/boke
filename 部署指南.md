# 个人博客部署操作指南

## 第一步：安装依赖

### 1. 打开终端/命令行

- **Windows**: 按 `Win + R`，输入 `cmd` 或 `powershell`，回车
- **Mac**: 按 `Cmd + Space`，输入 `terminal`，回车
- **或者**: 在项目文件夹中，按住 `Shift` 右键，选择"在此处打开 PowerShell" 或 "在此处打开命令窗口"

### 2. 进入项目目录

```bash
cd C:\Users\mzh20\Downloads\test
```

### 3. 安装依赖包

```bash
npm install
```

等待安装完成（可能需要几分钟），你会看到类似这样的输出：
```
added 123 packages in 2m
```

---

## 第二步：配置环境变量

### 1. 获取 Supabase 配置信息

1. 打开浏览器，访问 [https://supabase.com](https://supabase.com)
2. 登录你的账号
3. 进入你的项目（从图片中看到项目URL是 `gobttgepjyckujzdukp`）
4. 点击左侧菜单的 **Settings**（设置）
5. 点击 **API**
6. 找到以下两个值：
   - **Project URL** - 这是你的 `VITE_SUPABASE_URL`
   - **anon public** key - 这是你的 `VITE_SUPABASE_ANON_KEY`

### 2. 创建环境变量文件

在项目根目录（`C:\Users\mzh20\Downloads\test`）下：

**方法一：使用命令行**
```bash
copy env.example .env
```

**方法二：手动创建**
1. 在项目文件夹中找到 `env.example` 文件
2. 复制这个文件
3. 重命名为 `.env`（注意前面有个点）

### 3. 编辑 .env 文件

用记事本或其他文本编辑器打开 `.env` 文件，替换以下内容：

```env
VITE_SUPABASE_URL=https://gobttgepjyckujzdukp.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon_key值
```

**示例：**
```env
VITE_SUPABASE_URL=https://gobttgepjyckujzdukp.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdvYnR0Z2Vwanlja3VqemR1a3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTY5ODc2NTQzNCwiZXhwIjoyMDE0MzQxNDM0fQ.xxxxxxxxxxxxx
```

**⚠️ 重要提示：**
- 不要删除引号（如果有的话）
- 确保没有多余的空格
- 保存文件后关闭

---

## 第三步：启动开发服务器

### 在终端中运行：

```bash
npm run dev
```

### 你会看到类似这样的输出：

```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 打开浏览器

1. 复制显示的地址（通常是 `http://localhost:5173/`）
2. 在浏览器中打开
3. 你应该能看到你的博客网站了！

### 停止服务器

在终端中按 `Ctrl + C` 停止开发服务器

---

## 第四步：部署到 Netlify

### 准备工作：将代码推送到 Git 仓库

#### 1. 初始化 Git 仓库（如果还没有）

```bash
git init
```

#### 2. 创建 .gitignore（已存在，检查一下）

确保 `.gitignore` 文件包含 `.env`，这样不会把敏感信息上传

#### 3. 添加文件到 Git

```bash
git add .
```

#### 4. 提交代码

```bash
git commit -m "初始提交：个人博客项目"
```

#### 5. 在 GitHub/GitLab/Bitbucket 创建仓库

1. 登录 [GitHub](https://github.com)（如果没有账号，先注册）
2. 点击右上角 `+` → `New repository`
3. 输入仓库名称，例如：`my-personal-blog`
4. 选择 `Public` 或 `Private`
5. **不要**勾选 "Initialize with README"
6. 点击 `Create repository`

#### 6. 连接本地仓库到远程仓库

GitHub 会显示命令，类似这样：

```bash
git remote add origin https://github.com/你的用户名/my-personal-blog.git
git branch -M main
git push -u origin main
```

复制这些命令到终端执行

---

### 在 Netlify 部署

#### 方法一：通过网页界面（推荐）

1. **登录 Netlify**
   - 访问 [https://www.netlify.com](https://www.netlify.com)
   - 如果没有账号，点击 "Sign up" 注册（可以用 GitHub 账号登录）

2. **导入项目**
   - 登录后，点击右上角 **"Add new site"**
   - 选择 **"Import an existing project"**
   - 选择 **"Deploy with GitHub"**（或其他 Git 提供商）
   - 授权 Netlify 访问你的 GitHub 账号
   - 选择你刚创建的仓库

3. **配置构建设置**
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - 点击 **"Show advanced"** 可以设置更多选项

4. **设置环境变量**
   - 在部署设置页面，找到 **"Environment variables"** 或 **"Environment"**
   - 点击 **"Add variable"**
   - 添加两个变量：
     - **Key**: `VITE_SUPABASE_URL`，**Value**: 你的 Supabase URL
     - **Key**: `VITE_SUPABASE_ANON_KEY`，**Value**: 你的 Supabase Anon Key
   - 点击 **"Deploy site"**

5. **等待部署完成**
   - Netlify 会自动开始构建和部署
   - 通常需要 1-3 分钟
   - 部署完成后，你会得到一个网址，例如：`https://your-site-name.netlify.app`

#### 方法二：通过 Netlify CLI

1. **安装 Netlify CLI**

```bash
npm install -g netlify-cli
```

2. **登录 Netlify**

```bash
netlify login
```

会打开浏览器让你授权

3. **初始化项目**

```bash
netlify init
```

按照提示操作：
- 选择 "Create & configure a new site"
- 输入站点名称
- 选择团队（如果有）

4. **设置环境变量**

```bash
netlify env:set VITE_SUPABASE_URL "你的Supabase_URL"
netlify env:set VITE_SUPABASE_ANON_KEY "你的Anon_Key"
```

5. **部署**

```bash
netlify deploy --prod
```

---

## 常见问题解决

### 问题1：npm install 很慢或失败

**解决方案：**
```bash
# 使用国内镜像源
npm config set registry https://registry.npmmirror.com
npm install
```

### 问题2：找不到 .env 文件

**解决方案：**
- Windows 可能默认隐藏以点开头的文件
- 在文件资源管理器中，点击"查看" → 勾选"显示隐藏的文件"
- 或者直接用命令行创建

### 问题3：开发服务器启动失败

**检查：**
1. 是否安装了 Node.js（需要 16+ 版本）
   ```bash
   node -v
   ```
2. 是否在正确的目录
3. 是否安装了依赖（运行了 `npm install`）

### 问题4：Supabase 连接失败

**检查：**
1. `.env` 文件中的 URL 和 Key 是否正确
2. 是否重启了开发服务器（修改 .env 后需要重启）
3. Supabase 项目是否正常运行

### 问题5：Netlify 部署失败

**检查：**
1. 环境变量是否在 Netlify 中正确设置
2. 构建命令是否正确：`npm run build`
3. 发布目录是否正确：`dist`
4. 查看 Netlify 的构建日志，找到具体错误信息

---

## 完成后的检查清单

- [ ] 依赖安装成功
- [ ] .env 文件已创建并配置
- [ ] 本地开发服务器可以运行
- [ ] 网站可以正常显示数据
- [ ] 代码已推送到 Git 仓库
- [ ] Netlify 部署成功
- [ ] 环境变量已在 Netlify 中设置
- [ ] 网站可以正常访问

---

## 需要帮助？

如果遇到问题，可以：
1. 查看终端/命令行的错误信息
2. 检查浏览器控制台（F12）的错误
3. 查看 Netlify 的构建日志
4. 确认 Supabase 数据表结构是否匹配代码中的查询

祝你部署顺利！🎉

