# 环境变量问题排查指南

## 问题：环境变量已添加但仍显示未配置

### 根本原因

**Vite 的环境变量在构建时（Build Time）注入，不是在运行时（Runtime）读取！**

这意味着：
- ✅ 在 Netlify 中添加环境变量 → 只是保存了配置
- ❌ 不重新部署 → 代码中仍然没有环境变量的值
- ✅ 重新部署 → 构建时环境变量被注入到代码中 → 才能正常使用

---

## 解决步骤

### 1. 确认环境变量已正确添加

1. 登录 [Netlify Dashboard](https://app.netlify.com)
2. 进入你的网站项目
3. 点击 **"Site settings"** → **"Environment variables"**
4. 确认以下两个变量存在且值正确：

   ```
   VITE_SUPABASE_URL = https://gobttcgepiyckujzdakp.supabase.co
   VITE_SUPABASE_ANON_KEY = sb_publishable_xCnL6wI0soK72MOJfdRALw_j1SRzWDx
   ```

### 2. ⚠️ 关键步骤：重新部署

**这是最重要的步骤！**

1. 在 Netlify Dashboard 中，点击顶部的 **"Deploys"** 标签
2. 点击右上角的 **"Trigger deploy"** 按钮
3. 选择 **"Clear cache and deploy site"**（清除缓存并部署）
4. 等待部署完成（通常 1-3 分钟）
5. 部署完成后，刷新你的网站

### 3. 验证环境变量是否生效

部署完成后，刷新网站：
- ✅ 如果环境变量已生效：网站正常显示，不再显示错误提示
- ❌ 如果仍然显示错误：继续下面的排查步骤

---

## 进一步排查

### 检查 1：环境变量名称是否正确

确保变量名**完全一致**（区分大小写）：
- ✅ `VITE_SUPABASE_URL`（正确）
- ❌ `VITE_SUPABASE_url`（错误，大小写不一致）
- ❌ `SUPABASE_URL`（错误，缺少 VITE_ 前缀）

### 检查 2：环境变量值是否正确

- 检查 URL 是否完整（包含 `https://`）
- 检查 Key 是否完整（没有多余空格或换行）
- 不要在值的前后添加引号

### 检查 3：查看构建日志

1. 在 Netlify Dashboard → Deploys
2. 点击最新的部署
3. 查看构建日志（Build log）
4. 检查是否有错误信息

### 检查 4：清除浏览器缓存

有时浏览器缓存了旧版本：
- 按 `Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）强制刷新
- 或者使用无痕模式访问网站

### 检查 5：确认部署的是最新代码

1. 在 Netlify Dashboard → Deploys
2. 确认最新的部署是刚刚触发的
3. 检查部署时间是否在添加环境变量之后

---

## 常见错误

### 错误 1：只添加了环境变量，没有重新部署

**症状：** 环境变量已添加，但网站仍然显示未配置

**解决：** 必须重新部署！这是最常见的问题。

### 错误 2：环境变量添加到了错误的作用域

**症状：** 环境变量已添加，但构建时仍然读取不到

**解决：** 确保环境变量添加到 **"All scopes"** 或 **"Production"** 作用域

### 错误 3：变量名拼写错误

**症状：** 环境变量已添加，但代码读取不到

**解决：** 检查变量名是否完全匹配（包括大小写）

---

## 验证方法

部署完成后，可以在浏览器控制台（F12）中检查：

```javascript
// 在浏览器控制台中运行
console.log('URL:', import.meta.env.VITE_SUPABASE_URL)
console.log('Key:', import.meta.env.VITE_SUPABASE_ANON_KEY)
```

如果显示 `undefined`，说明环境变量仍未生效，需要重新部署。

---

## 需要帮助？

如果按照以上步骤操作后仍有问题，请提供：
1. Netlify 构建日志的截图
2. 浏览器控制台的错误信息（F12 → Console）
3. 环境变量设置的截图（隐藏敏感值）

